export default class Task {
  constructor(title, description, note, prioStatus, date) {
    this.title = title;
    this.description = description;
    this.note = note;
    this.prioStatus = prioStatus;
    this.date = date;
  }

  // getTitle, getDescription, getNote, getPrioStatus, getDate

  // setTitle, setDescription, setNote, setPrioStatus, setDate
}
